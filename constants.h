#pragma once

namespace netapi {
    const size_t constMaxNameSize = (1024*1024);
    const size_t constMaxQueueSize = (4*1024);
    const size_t constMaxDescriptorChainLength = (100);
} /* namespace netapi */
